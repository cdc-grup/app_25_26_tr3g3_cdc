# Database Connection
# Format: postgres://<user>:<password>@<host>:<port>
DATABASE_URL=postgres://postgres:password@localhost:5432/circuit_copilot
# Note: If port 5432 is busy, Docker might fail. Check docker-compose.yml.

# Application Port
PORT=3000
